// monaco-setup.js

// Import Monaco Editor
import * as monaco from 'monaco-editor';

// The Vite plugin will handle worker configuration automatically
console.log('[Monaco] Monaco Editor loaded with Vite plugin support');

// Export for potential use
export { monaco };
